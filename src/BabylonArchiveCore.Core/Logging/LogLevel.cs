namespace BabylonArchiveCore.Core.Logging
{
    /// <summary>
    /// Уровень важности лог-записи.
    /// </summary>
    public enum LogLevel
    {
        Trace,
        Debug,
        Info,
        Warning,
        Error,
        Fatal,
        UserNote  // Заметки пользователя (Мастера)
    }
}
