# Сессия 017 — Runtime State Manager

## Контекст
После базового game loop необходим явный менеджер runtime-состояния для синхронизации режима и тикового состояния.

## Выполнено
- Добавлен контракт `Session017RuntimeStateContract`
- Добавлены `session-017.schema.json`, `Session017Serializer`, `Migration_017`
- Добавлен `RuntimeStateManager` с sync/transition/dirty state
- Подготовлены тесты Core/Runtime/Integration/Session017

## Результат
Сформирован управляемый слой runtime-состояния для следующих сессий command/input контура.

## Статус
✅ Завершена
