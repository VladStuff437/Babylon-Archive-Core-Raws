- INV-039: Enemy state transitions must be deterministic for identical EnemyStateInput snapshots.
- INV-040: Damage formula must clamp invalid negative inputs and respect configured minimum damage.
- INV-041: Mission transition evaluator must select the highest-priority satisfied transition, otherwise deterministic fallback.
