- INV-054: Equivalent mission validation issue sets must map to identical fallback mission identity and metadata.
- INV-055: Mission runtime snapshots with checksum metadata must fail restore when integrity validation fails.
- INV-056: ArchiveAddress navigation and hierarchical seed derivation must be deterministic for identical address paths and world seeds.
