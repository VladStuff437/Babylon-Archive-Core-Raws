- INV-036: During mission transition lock, combat input must not mutate target selection state.
- INV-037: Auto-attack ticks must never emit strikes while mission transition lock is active.
- INV-038: Perception primary target selection must be deterministic for identical visibility snapshots.
