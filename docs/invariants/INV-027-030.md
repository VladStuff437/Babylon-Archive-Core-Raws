- INV-027: Combat cycle must follow phase order Direct → AOE → Status → Resolve.
- INV-028: Inventory capacity and stack limits must be enforced at runtime.
- INV-029: PlayerState attributes must be clamped to valid ranges.
- INV-030: AI state machine must transition to Dead when health <= 0.
