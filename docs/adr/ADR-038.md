# ADR-038: MissionNode Contract Finalization

## Status
Accepted

## Context
Session 038 finalizes MissionNode transition semantics with fallback rules and deterministic evaluator behavior.

## Decision
- Introduce Session038 mission-node contract/schema/migration/serializer/profile.
- Extend MissionNode validation with duplicate-target and fallback constraints.
- Extend TransitionEvaluator with deterministic fallback selection.

## Consequences
- Mission graph transitions are safer and more deterministic.
- Runtime selection logic remains stable under incomplete condition sets.
